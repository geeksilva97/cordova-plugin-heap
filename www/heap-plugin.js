"use strict";

var exec = require("cordova/exec");

/**
 * The Cordova plugin ID for this plugin.
 */
var PLUGIN_ID = "HeapIoPlugin";

/**
 * The plugin which will be exported and exposed in the global scope.
 */
var HeapIoPlugin = {};

module.exports = HeapIoPlugin;
